# Copie este arquivo para ".env" e ajuste os valores.
# O .env de verdade NÃO vai para o Git (está no .gitignore).
#
#   cp .env.example .env
#
DB_HOST=localhost
DB_NAME=gestao_alunos
DB_USER=qualquer
DB_PASSWORD=senha
